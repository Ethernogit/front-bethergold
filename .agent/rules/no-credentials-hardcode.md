---
trigger: always_on
---

# No hardcodear credenciales / No hardcoded credentials / Nessuna credenziale nel codice

- **ES:** Nunca poner credenciales, tokens, API keys o URLs privadas en archivos. Usar `src/environments/environment.ts`.
- **EN:** Never hardcode credentials, tokens, API keys or private URLs. Use `src/environments/environment.ts`.
- **IT:** Mai inserire credenziali, token, API key o URL privati. Usa `src/environments/environment.ts`.
